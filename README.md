# Reflect

模板类型的invoke改为数个重载版本，接受int，long，float，double以及void*，因为不能把多个调用放在一个模板中，对于一个调用模板参数会进行推断，这个参数必须匹配所有的反射方法，这是不可能的


## version_2017_04_16_


## version_2017_4_29(增加配置文件解析)

## version_2017_4_29(完成完整的反射(初版))

## version_2017_5_1(完成完整的反射(终版))